# Database
DATABASE_URL=postgresql+asyncpg://app:changeme@postgres:5432/reel_processor
POSTGRES_USER=app
POSTGRES_PASSWORD=changeme
POSTGRES_DB=reel_processor

# Redis
REDIS_URL=redis://redis:6379/0

# Auth
SECRET_KEY=change-this-to-a-random-secret-key

# ML
WHISPER_MODEL=base

# Frontend (Vite dev server proxy target — only needed in Docker)
# VITE_API_PROXY_TARGET=http://api:8000
